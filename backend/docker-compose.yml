services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wayfinder-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
    networks:
      - wayfinder-network

networks:
  wayfinder-network:
    driver: bridge
